apiVersion: v1
kind: Service
metadata:
  name: dev-service
  labels:
    app: social-media
    env: development
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: social-media
    env: development 